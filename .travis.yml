language: node_js

node_js:
  - "4.5.0"

before_install:
  - "npm install -g grunt-cli"
  - "npm install -g bower"

before_script:
  - "npm install"
  - "bower install"

script:
  - "npm run coverage"
